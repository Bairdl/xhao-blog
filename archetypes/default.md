+++
title = '{{ replace .File.ContentBaseName "-" " " | title }}'
date = '{{ .Date }}'
author =  "XHao"  # 文章作者
# weight = 1  # 文章权重（数值越大排序越靠前）
# aliases = ["/draft-example"]  # 文章别名（可用于重定向）
# tags = ["template"]  # 文章标签（数组形式）
# categories = [""] # 文章分类
description = "Default description."  # 文章描述（用于摘要显示）
draft = true  # 是否为草稿（true表示草稿，不会构建发布）
+++
